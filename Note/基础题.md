## 4月27日
>https://www.nowcoder.com/exam/oj/ta?tpId=383

![](https://cdn.jsdelivr.net/gh/March030303/Picgo@main/img/20260502220024709.png)
我的代码：
```
import java.util.Scanner;
public class Main {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        int[] arr=new int[20];
        int n=in.nextInt();
        arr[0]=0;
        arr[1]=1;
        arr[2]=1;
        arr[n]=arr[n-3]+2*arr[n-2]+arr[n-1];//甚至没存起来
System.out.println(arr[n]);    
    }
}
```
我第二次的代码：
```
#include <bits/stdc++.h>
using namespace std;
int f(int n){
    if(n==1){
        return 0;
    }
    if(n==2||n==3){
        return 1;
    }
    
    f(n)=f(n-3)+2*f(n-2)+f(n-1);//f(n)的返回值是临时值，不能这样赋值又当结果使用
    
return f(n);
}
int main() {
   int n;
   cin>>n;
    cout<<f(n);
   

   return 0;
}
```


<span style="color:red"> 

多种正确思路应该是：
1、递归
2、把前面的数存进sum里，然后每次取sum[i-3]+2*sum[i-2]+sum[i-1]
3、缓存

顺带一提，递归函数没有f(1)=1这样赋值的，应该是类似n\==1的时候返回0，n\==2||n\==3返回1这样子

</span>

![](https://cdn.jsdelivr.net/gh/March030303/Picgo@main/img/20260502220136686.png)
```
import java.util.Scanner;

public class Main {
    static final int MOD = 1000000007;//要有全局变量的意识
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int n = scanner.nextInt();
        int m = scanner.nextInt();
        
        long[][] dp = new long[n + 1][m + 1];
        
        for (int i = 1; i <= n; i++)
        {
            dp[i][1] = 1;
        }
        for (int j = 1; j <= m; j++)
        {
            dp[1][j] = 1;
        }
        
        for (int i = 2; i <= n; i++)
        {
            for (int j = 2; j <= m; j++)
            {
                dp[i][j] = (dp[i - 1][j] + dp[i][j - 1]) % MOD;
            }
        }
        System.out.println(dp[n][m]);
    }
}
```


## 5月2日
![](https://cdn.jsdelivr.net/gh/March030303/Picgo@main/img/20260502220804691.png)
思路：
1、动态定义数组、并用scanner输入数据
==2、遍历（这样会有每次的索引），再与随机索引（random）的元素交换位置==
3、输出数组
```
import java.util.Scanner;
import java.util.Random;
public class Random_array {
    static void main(String[] args) {
        //初始化一个数组
        //录入
        //遍历后，随机索引，然后交换
        int[] arr=new int[5];
        Scanner in=new Scanner(System.in);
        for (int i=0;i<arr.length;i++){
            System.out.println("请输入第" + (i + 1) + "个学生的学号");
            arr[i]=in.nextInt();
            
        }
        Random r=new Random();
        for (int i = 0; i < arr.length; i++) {
            int index=r.nextInt(arr.length);
            int temp=arr[i];
            arr[i]=arr[index];
            arr[index]=temp;
        }
        for (int i = 0; i < arr.length; i++) {
            System.out.println("第" + (i + 1) + "个同学打乱后的学号是" + arr[i]);

        }

    }
}
```
![](https://cdn.jsdelivr.net/gh/March030303/Picgo@main/img/20260502220735415.png)
```
import java.util.Scanner;
public class Array2 {
    static void main(String[] args) {
        Scanner in=new Scanner(System.in);
        int n=in.nextInt();
        int[] a=new int[n];
        for (int i = 0; i < a.length; i++) {
            a[i] = in.nextInt();
        }
        int[] b=new int[n];
        for (int i = 0; i < a.length; i++) {
            for (int j = 0; j < i; j++) {
                if(a[i]>a[j]){
                    b[i]++;
                }
            }
        }
        for (int i = 0; i < b.length; i++) {
            System.out.println(b[i]);
        }
    }
}

```
## 5月3日
1、题目如果让写一个两个int相乘的函数，不仅要注意结果不能被int装下，还要注意int的范围，不能直接相乘，否则会溢出。
思路是：==(long)x*y==这样先把其中一个强制转换为long，根据计算时有大则大的原则，结果自然会存为long类型
不用在意强制转换谁，效果都是一样的

2、1e9其实是double类型的，所以要根据需要强制转换
![](https://cdn.jsdelivr.net/gh/March030303/Picgo@main/img/20260504003824680.png)